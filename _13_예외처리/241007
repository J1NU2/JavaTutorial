JVM : Java 바이너리 파일 실행

Java 예외처리
　- 에러와 예외를 구분하는 것이 좋다.
　- 에러(Error) 예외(Exception)

자바는 컴파일언어이자 객체지향언어이다.
　- 에러 : 컴파일언어는 에러가 발생하면 컴파일이 되지 않는다. (실행(동작)이 되지 않는다.)
　- 예외 : 실행(동작) 중 예기치 못한 이슈가 발생한 상황이다.
	이슈가 발생해도 실행이 되기 때문에 오작동이 된다.
	　→ 이러한 일을 Java에선 사전에 방지하는 것이다.
Java에서는 예외를 처리해주는 객체가 존재한다.
	Exception이라는 객체에서 정의가 되어있다.

공부를 하며 많이 봤던 Exception
　- NullPointerException ~
　- IndexOutOfBoundsException ~
　- TypeMissMatch ~

예외처리 방법
　- 직접 처리 : try-catch 구문
　- 떠넘기기(누구에게 call한 위치로) : 메서드명() throws exception
	메서드에서 예외(Exception) 발생 시 나를 호출(call)한 메서드가 처리

